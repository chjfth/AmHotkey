const char *intdiv = "aa // bb" ; // nice day
const char *s2 /*.".*/ = "cc // dd" ;
Win_Client_X := NumGet(&RECT, 0, "Int") ; comment with semicolon
// a comment with "double-quoted" string
// a comment with 'single-quoted' string
//

int a = 1;
const char *s = // trailing comment
"String content";
void func() { }

int foo1()
{
	// need to have "4 spaces" ahead.
	return 0;
	/* Block comment "across"
		multiple
	lines. */
}
